const Header = () => {
	const template = `
    <header>
        <h1>My Parcel app</h1>
        <p>This is a boilerplate for JS workflow using Parcel</p>
    </header>
    `;

	return template;
};

export default Header;
